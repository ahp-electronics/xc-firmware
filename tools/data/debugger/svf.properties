# Define SciTE settings for edif files.

# edif files
file.patterns.svf=*.svf
filter.svf=svf (svf)|$(file.patterns.svf)|

lexer.$(file.patterns.svf)=svf

word.chars.svf=$(chars.alpha)$(chars.numeric)_
word.characters.$(file.patterns.svf)=$(word.chars.svf)


comment.block.svf=!//
#comment.block.at.line.start.edif=1
#comment.stream.start.edif={
#comment.stream.end.edif=}
#comment.box.start.edif=/*
#comment.box.middle.edif= *
#comment.box.end.edif= */

#~ fold.at.Begin=1
#~ fold.at.Parenthese=1

#statement.lookback.$(file.patterns.edif)=20
#block.start.$(file.patterns.edif)=5 begin
#block.end.$(file.patterns.edif)=5 end
#statement.indent.$(file.patterns.edif)=5 always case casex casez else for if while \
#module function task

indent.maintain.$(file.patterns.svf)=1;


keywords.$(file.patterns.svf)=TRST ENDDR ENDIR RUNTEST TDI TDO MASK SMASK RMASK CMASK DMASK CRC HDR TDR HIR TIR SIR SDR STATE FREQUENCY LCOUNT LDELAY LSDR LVDS ISPEN SOURCE LOOP ENDLOOP

keywords2.$(file.patterns.svf)= 
# edif styles

# Default
style.svf.32=$(font.base)
# White space
#style.svf.0=fore:#800080
# Comment
style.svf.1=$(colour.code.comment.line),$(font.code.comment.line)
# Bang comment
#style.svf.2=fore:#3F7F3F,$(font.code.comment.line)
# Number
#style.svf.3=$(colour.number)
# Double quoted string
style.svf.4=$(colour.string)
# Operators
#style.edif.5=$(colour.operator)
# Identifiers
#style.svf.6=
# End of line where string is not closed
#style.svf.7=fore:#000000,$(font.string.literal),back:#E0C0E0,eolfilled
# Keyword
style.svf.8=$(colour.keyword)
# Std operator
#style.svf.9=fore:#007F7F
# Attribute
#style.svf.10=fore:#804020
# Std Function
#style.svf.11=fore:#808020

